# 六环多目标搜索 POC 报告

状态：历史 POC 基线；2026-08-05 单调跨界约束改造后的真实数据复测待执行
验证日期：2026-08-04

> 目录迁移说明（2026-08-06）：可重新生成的 POC 产物已从运行数据根迁移到仓库同级 `workspace/work`；下文路径已更新为迁移后的实际位置，验证统计仍对应 2026-08-04 的原始运行。

## 1. 结论

使用有向 edge key 作为状态的单次多目标 Dijkstra 可以实现已冻结的两级选择规则：先求最短环内合规距离 `Dmin`，仅保留 `Dinside <= Dmin + 1000 米` 的通行口，再在候选集中按完整参考路线距离选择大方向。

本次已使用生产拓扑构建器生成的新候选在默认 `COMPLIANT_DISTANCE_V1` 图上重跑。搜索沿用 Profile 的汽车可达性、2,359 条 OSM 转向限制和 `road_access` 入口规则，同时按真实边长累计米数；正向出环和反向入环分别用 178.4 ms、132.0 ms 收敛，330/384 个原始有向候选都缩小到 3 个。

## 2. 搜索模型

- 正向出环：从环内点按正向驾驶方向搜索 `OUTBOUND` edge-key 候选。
- 反向入环：从环内终点反向搜索 `INBOUND` 候选；转向代价仍按正向驾驶顺序计算。
- 状态：有向 edge key，而不是节点；同一节点通过不同道路到达时保持独立状态。
- 通行口：边内部交点使用沿有向边的比例；边端和边界重叠链候选保留目标边、边界节点及实际离开边界的比例。
- 计量：基础 Profile 判断边是否可达并提供禁转/受限入口代价；合法普通边使用实际米数累加。
- 终止：首个通行口确定 `Dmin`，继续搜索至队列最小距离超过 `Dmin + 1000 米`。
- 硬约束：每次扩展经过 `BlockedEdgeWeighting`；成功候选再次检查 edge key 和 JTS 几何零冲突。
- 错误：只有队列耗尽且没有候选才证明无路；超时和访问状态上限是独立完成状态。

## 3. 合成图验证

自动测试已覆盖：

1. 候选与最短距离相差 999 米时保留，相差 1001 米时排除。
2. 最短到达状态遇到禁止转向时，保留同节点另一条合法到达 edge key 并找到绕行。
3. 反向搜索按正向驾驶顺序检查禁止转向。
4. 摄像头命中的有向边在多目标搜索阶段被阻断。
5. 图耗尽无路与访问状态上限中断返回不同结果。

GraphHopper 11 的 `DijkstraOneToMany` 不适用：node-based 模式拒绝带 turn costs 的权重，也无法表达上述多到达边状态。

## 4. 真实图输入

- 图指纹：`sha256:23c0009a6943af69973808f5d446698c6ebd3cc8a222b831025a59195a8ddafc`
- 摄像头源 SHA-256：`0c16ed44ef2e24cd4759afba28d6448925db699a4d3b38b05980f076e6ac7404`
- 禁行边版本：`sha256:870b1515b7ebbee677b8bd0050c3f9485da775b0bde14b2c1e5d973c1a661b3c`
- 禁行基础边：19,771
- 环内控制点：`116.3970000, 39.9080000`
- 环外参考点：`117.2009830, 39.0841580`
- 环内点吸附距离：1.5 米
- 权重：合规距离米数，启用 turn costs 与受限道路入口规则

## 5. 多目标结果

| 方向 | 原始候选 | `Dmin + 1km` 候选 | Dmin | 访问状态 | 耗时 | 禁边拒绝 |
|---|---:|---:|---:|---:|---:|---:|
| OUTBOUND | 330 | 3 | 31,773 m | 80,857 | 178.4 ms | 5,742 |
| INBOUND | 384 | 3 | 34,822 m | 92,821 | 132.0 ms | 6,447 |

两次搜索的完成状态均为 `TOLERANCE_SETTLED`，没有触发 30 秒超时或 2,000,000 状态上限。6 条入选环内路径均通过禁行 edge key 检查和独立 JTS 摄像头零冲突校验。

## 6. 完整参考路线决胜

| 方向 | 候选 | 环内距离 | 环外参考段 | 完整参考距离 | 结果 |
|---|---|---:|---:|---:|---|
| OUTBOUND | P0209 | 32,369 m | 112,113 m | 144,482 m | 选中 |
| OUTBOUND | P0205 | 31,773 m | 112,907 m | 144,680 m | 候选 |
| OUTBOUND | P0203 | 31,805 m | 113,494 m | 145,299 m | 候选 |
| INBOUND | P0571 | 35,069 m | 111,689 m | 146,757 m | 选中 |
| INBOUND | P0568 | 34,822 m | 112,291 m | 147,113 m | 候选 |
| INBOUND | P0566 | 34,849 m | 113,837 m | 148,686 m | 候选 |

出环和入环结果都没有机械选择 `Dmin`，而是在 1 公里容差内选择完整参考路线最短者，符合已冻结的业务语义。

## 7. 四方向扩展结果

已增加东、南、西、北各一条出环、入环和环内固定路线，共 12 个场景。当前缓存与缓存重载两轮选择完全一致，8 条跨界路线均选择预期象限，12 条完整路线均通过摄像头零冲突校验。首轮多目标搜索耗时为 10.4 至 81.4 ms。

南向入环路线选择了人工保留簇 `C123` 中的 `P0566`。该结果不会使技术回归失败，但生产接入前必须保留专项复核门槛。完整结果见 [六环东南西北真实路线回归报告](六环东南西北真实路线回归报告.md)。

## 8. 限制与下一步

- 当前固定场景不能据此冻结全市性能预算或导航质量。
- 20 个高复杂度位置已抽查 16 个初步通过、4 个保留，特殊拓扑候选和路线形状仍待发布前人工审批。
- 当前实现已升级到 OpenAPI 1.3，并增加搜索期单调边界约束、拓扑连续参考路线、方向相关的道路安全导航交接点和前端完整/环内视图；2026-08-06 正式规划器 14 条真实路线已复测通过，其中武清至朱辛庄入环路线要求在驶离六环高速后生成安全段交接点。人工路线形状、特殊拓扑和保留位置门槛仍在发布前执行。

## 9. 复现

- 历史算法 POC：`server/src/test/java/cn/camera/safe/routing/EdgeKeyMultiTargetDijkstraPoc.java`
- 生产算法：`server/src/main/java/cn/camera/safe/routing/EdgeKeyMultiTargetDijkstra.java`
- 正式规划器真实回归：`server/src/test/java/cn/camera/safe/routing/SixthRingRoutePlannerIntegrationTest.java`
- 生产算法合成图测试：`server/src/test/java/cn/camera/safe/routing/EdgeKeyMultiTargetDijkstraTest.java`
- 真实图测试：`server/src/test/java/cn/camera/safe/routing/SixthRingMultiTargetSearchPocTest.java`
- 外部完整报告：`F:\CodexProjects\routing-plan\workspace\work\sixth-ring-poc\20260804-production-topology-v1\sixth-ring-multitarget-search-report.md`
