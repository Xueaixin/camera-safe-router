# 历史资料

本目录保存首期并行开发产生的依赖调查、前后端交付说明和旧路网验证材料，仅用于追溯实现过程。

- 其中的 `Beijing.osm.pbf`、`../../data` 路径、旧文件哈希和 5,930/6,797 匹配结果均属于旧数据基线。
- 当前 PBF 名称、外部数据目录、启动方式和测试命令以 `code/README.md` 及 `code/docs` 当前文档为准。
- 历史文件不得作为新 Agent 的唯一任务说明，也不得据此回退当前契约或实现。

