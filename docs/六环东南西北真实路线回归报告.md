# 六环东南西北真实路线回归报告

状态：自动回归通过，路线形状人工抽查和保留位置复核尚未完成  
验证日期：2026-08-04

## 1. 结论

当前 `COMPLIANT_DISTANCE_V1` 图已经完成东、南、西、北各一条出环、入环和环内路线的真实数据回归，共 12 个场景。当前缓存与缓存重载两轮结果完全一致，所有成功路线的禁行 edge key 和独立 JTS 摄像头冲突数均为零。

四象限复杂位置人工抽查已完成 20 个样本，其中 16 个初步通过，`C004`、`C053`、`C060`、`C123` 作为保留位置。保留位置不从候选集中静默删除；本次南向入环路线实际选择了 `C123` 中的候选 `P0566`，因此该路线可以证明算法和安全约束正常工作，但不能作为 `C123` 已获人工批准的证据。

原始两轮结果属于生产接入前 POC 回归。此后 OpenAPI 1.1、生产路线服务和前端协议已接入六环多目标算法，并使用同一组 12 条夹具直接通过正式 `SixthRingRoutePlanner` 回归；候选边界仍未获生产批准。

## 2. 输入与产物

- PBF：`E:\camera-safe-routing-data\osm\jingjinji-latest.osm.pbf`
- 图缓存：`E:\camera-safe-routing-data\graph-cache\jingjinji-compliant-distance-v1`
- 图指纹：`sha256:23c0009a6943af69973808f5d446698c6ebd3cc8a222b831025a59195a8ddafc`
- 摄像头源 SHA-256：`0c16ed44ef2e24cd4759afba28d6448925db699a4d3b38b05980f076e6ac7404`
- 禁行基础边：19,771
- 固定夹具：`server/src/test/resources/fixtures/sixth-ring-directional-route-regression.json`
- 外部完整报告：`E:\camera-safe-routing-data\work\sixth-ring-poc\20260804-production-topology-v1\sixth-ring-directional-route-regression-report.md`
- QGIS 路线产物：`E:\camera-safe-routing-data\work\sixth-ring-poc\20260804-production-topology-v1\sixth-ring-directional-route-regression.geojson`

外部 GeoJSON 包含 12 条路线 LineString 和 8 个已选通行口 Point，属性中保留规划模式、候选 ID、edge key、边界角色、Dmin、环内距离、完整距离、搜索状态、冲突数和保留簇标记。

## 3. 跨界路线结果

| 场景 | 实际象限 | 候选 | Dmin | 环内距离 | 完整距离 | 冲突 | 保留项 |
|---|---|---|---:|---:|---:|---:|---|
| east-outbound | EAST | P0330 | 11,369 m | 11,677 m | 32,314 m | 0 | - |
| east-inbound | EAST | P0699 | 13,302 m | 13,401 m | 26,442 m | 0 | - |
| south-outbound | SOUTH | P0215 | 5,079 m | 5,079 m | 35,029 m | 0 | - |
| south-inbound | SOUTH | P0566 | 5,042 m | 5,350 m | 32,584 m | 0 | C123 |
| west-outbound | WEST | P0119 | 10,307 m | 10,307 m | 42,097 m | 0 | - |
| west-inbound | WEST | P0463 | 9,836 m | 9,836 m | 42,203 m | 0 | - |
| north-outbound | NORTH | P0214 | 10,192 m | 10,192 m | 62,272 m | 0 | - |
| north-inbound | NORTH | P0579 | 10,012 m | 10,012 m | 64,472 m | 0 | - |

每条路线先运行 edge-based 多目标 Dijkstra，保留 `Dinside <= Dmin + 1000 米` 的通行口，再使用完整参考路线距离和预计时长确定稳定顺序。所有已选候选的方向、边界角色和象限均与夹具预期一致。

## 4. 环内路线结果

| 场景 | 距离 | 冲突 |
|---|---:|---:|
| east-internal | 22,489 m | 0 |
| south-internal | 28,328 m | 0 |
| west-internal | 43,494 m | 0 |
| north-internal | 40,335 m | 0 |

环内路线继续使用默认距离主导 Profile、turn costs、`road_access` 规则和摄像头搜索期硬禁边，不经过六环通行口选择。

## 5. 缓存和性能

- 当前缓存和关闭后重新加载缓存的 12 条路线选择完全一致。
- 两轮跨界路线的候选 ID、完整距离整数米、实际象限和冲突数一致。
- 首轮多目标搜索耗时为 10.4 至 81.4 ms；缓存重载轮为 2.3 至 19.3 ms。
- 所有环内控制点均已校准到当前可驾车路网，吸附距离为零。

性能数字仅作为当前设备和输入版本的回归证据，不作为生产 SLA。

## 6. 当前边界与下一步

已通过：

1. 东、南、西、北出环和入环业务方向。
2. `Dmin + 1000 米` 候选保留和完整参考路线决胜。
3. 环内距离主导路线。
4. 搜索期禁行、提取路径检查和独立 JTS 零冲突。
5. 当前缓存与缓存重载的一致性。

仍需完成：

1. 在 QGIS 中人工查看 12 条最终路线形状。
2. 后续复核 `C004`、`C053`、`C060`、`C123`，其中 `C123` 已被南向入环回归实际选中。
3. 完成特殊拓扑候选和多目标最终候选的发布前人工核验。
4. 正式接口接入已完成；前端当前使用完整 `geometry` 兼容绘制，分段样式按产品需要后续增加。

## 7. 复现

POC 两轮缓存一致性入口：`server/src/test/java/cn/camera/safe/routing/SixthRingDirectionalRouteRegressionPocTest.java`。

正式规划器入口：`server/src/test/java/cn/camera/safe/routing/SixthRingRoutePlannerIntegrationTest.java`。

运行时需要提供：

- `real.pbf`
- `real.graph.cache`
- `real.camera.json`
- `real.routing.profile.mode=COMPLIANT_DISTANCE_V1`
- `sixth.ring.directed.input`
- `sixth.ring.boundary.input`
- `sixth.ring.clusters.input`
- `sixth.ring.regression.report.output`
- `sixth.ring.regression.geojson.output`
